# 任务清单：乡音地图 HarmonyOS App

## 任务 1：创建最小 HarmonyOS 工程骨架

**描述：** 建立 Stage 模型 `entry` 模块、应用配置、资源目录、Ability 和最小首页，使工程首先达到可识别、可编译状态。

**验收条件：**

- [x] 工程包含根级与 `entry` 级 Hvigor 配置。
- [x] `EntryAbility` 能加载首页路由。
- [x] 应用名称、包名和图标资源使用乡音地图标识。

**验证：**

- [x] 执行 `hvigorw assembleHap --no-daemon --stacktrace`。
- [x] 已产出 `entry/build/default/outputs/default/entry-default-unsigned.hap`；当前未配置签名。

**依赖：** 无
**预计文件：** 根配置、`AppScope` 配置、`entry` 配置、Ability、最小页面
**规模：** M（4-5 个逻辑文件组）

## 任务 2：定义展示模型和模拟服务

**描述：** 定义地图点位、同乡用户、鉴定结果和乡音护照模型，提供确定性的本地模拟数据和范围校验。

**验收条件：**

- [x] 所有百分比分数限制在 `0..100`。
- [x] 同乡列表包含附近、推荐和当前区域三种来源。
- [x] 模拟服务不请求网络、位置或麦克风权限。

**验证：**

- [x] 单元测试已编译进 `entry-ohosTest-unsigned.hap`；当前无连接设备，尚未执行。
- [x] 主 HAP 与 `ohosTest` HAP 均通过 Hvigor 构建。

**依赖：** 任务 1
**预计文件：** `models/DialectModels.ets`、`services/MockDialectService.ets`、对应测试
**规模：** M（3 个文件）

## 任务 3：首页地图与同乡面板

**描述：** 实现本地先行的地图主视觉、统一经纬度投影的乡音点位、缩放/平移/定位和可折叠底部面板，展示同乡与乡音群聊。

**验收条件：**

- [x] 地图支持缩放、平移和恢复定位。
- [x] 点位显示方言与同乡数量。
- [x] 本地地图立即显示；在线底图与 Marker 成功后才接管。
- [x] 本地底图和点位使用同一经纬度到裁剪图像坐标投影。
- [x] 面板支持折叠/展开并切换附近、推荐、当前区域。

**验证：**

- [x] 执行 Hvigor 构建。
- [ ] 使用当前构建在设备上检查本地首帧、在线成功接管、失败保持、点位投影与面板状态。

**依赖：** 任务 2
**预计文件：** 首页、地图组件、面板组件、地图资源
**规模：** M（4 个文件）

## 任务 4：同乡用户到乡音护照

**描述：** 从任一同乡用户卡片进入乡音护照，展示身份、方言指标、代表语音、点亮地区和社交统计。

**验收条件：**

- [x] 点击同乡卡片进入对应护照。
- [x] 护照完整展示规格中的九类信息。
- [x] 代表语音和分享入口提供可见的演示状态。

**验证：**

- [x] 执行 Hvigor 构建。
- [x] 护照由同一 `FellowUser` 模型生成，静态检查数据源一致；运行态待设备验证。

**依赖：** 任务 3
**预计文件：** 护照页、护照组件、路由参数处理
**规模：** M（3 个文件）

## 任务 5：模拟录音鉴定与地区点亮

**描述：** 从首页进入录音状态机，完成模拟鉴定、地区确认、点亮地区，并生成当前用户乡音护照。

**验收条件：**

- [x] 录音具有准备、录音中、完成和鉴定结果状态。
- [x] 结果包含方言归属、置信度和乡音保留度。
- [x] 页面明确显示“模拟鉴定”。
- [x] 点亮地区后可进入当前用户护照。

**验证：**

- [x] 鉴定服务和护照生成测试已编译进 `ohosTest` HAP，待设备执行。
- [x] 主 HAP 与测试 HAP 均通过 Hvigor 构建。
- [ ] 完整核心流程待 Previewer 或设备人工走通。

**依赖：** 任务 2、任务 4
**预计文件：** 鉴定页、状态组件、服务测试、首页状态连接
**规模：** M（4 个文件）

## 任务 6：建立仓颉领域模型与测试草案

**描述：** 依据 CangjieSkills 建立独立 `cangjie-domain`，定义方言鉴定、地区点亮和乡音护照的不可变领域模型及测试。

**验收条件：**

- [x] 项目结构、命名和格式符合 `cangjie-regulations`。
- [x] 使用 `Option<T>` 处理可预期的缺失值。
- [x] 百分比分数经过边界校验。
- [x] README 明确当前未编译原因和未来接入方式。

**验证：**

- [x] 已执行目录、包声明、命名、行长、敏感字段和 `git diff --check` 静态检查。
- [x] 未检测到 `cjc/cjpm`，仓颉编译和测试明确记录为未验证。

**依赖：** 任务 2
**预计文件：** `cjpm.toml`、两个领域包、测试、README
**规模：** M（4-5 个文件）

## 任务 7：资源、文档和最终验证

**描述：** 完成应用资源、README、能力边界说明和最终构建，形成可交付工程。

**验收条件：**

- [x] README 包含构建命令、功能范围与模拟能力说明。
- [x] 不包含密钥、Token、真实精确位置或录音数据。
- [x] 构建结果、签名状态和设备运行状态分开记录。

**验证：**

- [x] 主 HAP 与 `ohosTest` HAP 最终 Hvigor 构建均成功；测试仅完成编译，未在设备执行。
- [x] 已搜索敏感字段与误导性能力描述，命中均为明确的否定边界说明。
- [x] 已检查两个 unsigned HAP，并记录路径、大小与 SHA-256。

**依赖：** 任务 1-6
**预计文件：** README、资源、构建记录、任务状态
**规模：** M（3-5 个文件）

## 任务 8：可解释鉴定与用户确认纠正

**描述：** 扩充模拟鉴定数据契约，展示方言大区、分支、候选地域、识别依据和相似乡音；用户确认、纠正或重新录制后才能进入点亮流程。

**验收条件：**

- [x] 模拟结果提供可解释字段且持续显示模拟声明。
- [x] 用户可以确认四川话、纠正为重庆话或重新录制。
- [x] 未确认结果不能生成乡音护照。

**验证：**

- [x] 主 HAP 通过 Hvigor 构建。
- [x] `ohosTest` HAP 通过 Hvigor 编译打包。
- [ ] 确认、纠正和重新录制三条路径待真机人工走通。

**依赖：** 任务 5
**预计文件：** 鉴定模型、模拟服务、鉴定组件、模型测试
**规模：** M（4 个文件）

## 任务 9：持久化护照与地图点亮

**描述：** 使用 Preferences 保存用户确认的地区、方言和分支，应用再次启动时恢复当前用户护照和地图点亮状态。

**验收条件：**

- [x] 确认结果后保存最小必要字段，不保存录音和精确位置。
- [x] 本地地图与 Map Kit 点位使用独立的已点亮视觉。
- [x] 读取或写入失败时安全降级，不阻断本次流程。

**验证：**

- [x] 主 HAP 通过 Hvigor 构建。
- [ ] 重启应用后的护照与地图恢复待真机验证。

**依赖：** 任务 8
**预计文件：** Preferences 服务、首页状态、地图组件、点亮资源
**规模：** M（5 个文件）

## 任务 10：逻辑一致性、安全与返回层级修复

**描述：** 清除受跟踪签名材料；让鉴定确认和纠正结果保持领域字段一致；未鉴定时门控当前用户护照；提升分享预览和纠正面板状态；刷新 Map Kit Marker 点亮状态。

**验收条件：**

- [x] `build-profile.json5` 的 `signingConfigs` 为空，Map Kit capability 保留。
- [x] 普通确认后 `needsConfirmation` 为 `false`。
- [x] 纠正为重庆话后，候选地域、依据、相似乡音和分类字段均切换为重庆模拟详情。
- [x] 没有恢复状态或已确认鉴定时，不创建默认四川护照。
- [x] 系统返回依次关闭分享预览、纠正面板、鉴定页和护照页。
- [x] Map Kit Marker 使用当前 SDK 的实例更新 API 响应再次鉴定。

**验证：**

- [x] 主 HAP 通过 Hvigor 类型检查与 unsigned 打包。
- [x] 回归测试已编译进 `ohosTest` HAP。
- [x] 已扫描受跟踪源码与配置中的签名材料字段。
- [ ] `ohosTest` 用例尚未在设备实际执行。
- [ ] 当前源码的返回层级、护照门控、纠正详情和 Marker 刷新尚未真机复验。

**依赖：** 任务 8、任务 9
**预计文件：** 根配置、模拟服务、页面状态、地图组件、测试与文档
**规模：** M（多文件定向修复）

## 当前优先队列：质感、群组、小艺与真实数据

> 状态：规格已确认，实施中。以下任务优先于原设备验收队列。

## 任务 14：统一视觉 Token 与首页信息架构

**描述：** 以“宣纸上的乡音聚落”为视觉命题，使用宣纸暖白与黛青统一 Light/Dark 语义资源；在地图同乡面板加入固定的“同乡/乡音群聊”一级切换，并优化层级、状态、焦点和响应式布局。

**验收条件：**

- [x] 地图仍是首页主视觉，小艺入口不遮挡地图或替代固定导航。
- [x] 同乡/乡音群聊一级切换与各自二级筛选层级清晰。
- [x] 宣纸暖白作为页面基底，黛青承担品牌与主操作；文字/填充 Token 分离。
- [x] 内容面使用细边框与微阴影，不以重阴影和同层卡片套卡片制造层级。
- [x] 主要热区不小于 48×48vp，字体使用 fp，Light/Dark 语义一致。
- [x] 手机竖屏为底部面板，横屏大窗口为地图 + 侧栏。

**验证：**

- [ ] 当前视觉资源与组件改动需统一执行主 HAP 构建。
- [ ] 当前宣纸暖白/黛青、边框/微阴影和群聊信息架构需重新取得横竖屏真机证据。

**依赖：** 阶段 6 规格（已确认）
**预计文件：** 颜色资源、设计系统、首页、同乡面板和新群组入口组件
**规模：** M

## 任务 15：群组本地演示闭环

**描述：** 将旧群组介绍页重构为微信式会话列表与消息流，提供有成员门槛、有界且只在本次运行有效的本机发送。

**验收条件：**

- [x] 默认“会话”展示已加入群组；推荐和按方言作为发现入口。
- [x] 会话行展示群头像、群名、最后消息、时间和演示未读/本机状态。
- [x] 群聊页展示时间分隔、示例发送者、本人气泡、输入框和发送按钮。
- [x] 加入/退出前二次确认；未加入或退出后禁用输入并提示“加入后可发言”。
- [x] 打开群聊记录 session `lastReadMessageId`，返回列表后清除对应演示未读角标。
- [x] 空白消息拒绝；单条最多 120 字，每群本次运行最多 100 条，满额后禁用并提示。
- [x] 本人消息仅保留本次运行，不自动回复、不伪装服务器送达或真实成员。
- [x] 聊天页使用 `RESIZE_WITH_CARET` 并在离开时恢复原键盘避让模式。

**验证：**

- [x] 筛选、成员状态、存储结果、session 已读和 100 条边界测试已写入源码。
- [ ] 当前测试尚需重新编译和执行，不沿用旧群组介绍页的测试结果。
- [ ] 会话列表、消息流、加入/退出、未读清零、满额、键盘与系统返回尚需当前构建真机复验。

**依赖：** 任务 14
**预计文件：** 群组模型、演示服务、列表组件、详情组件、首页状态和测试
**规模：** L（按模型/列表/详情三个增量交付）

## 任务 16：小艺能力适配与真实接入

**描述：** 建立未配置、检查中、不支持、可用和错误五态适配层；在 API 20 下配置用户提供的 `agentId` 并接入官方 `AgentFrameworkKit`，真机支持状态仍由官方检查决定。

**验收条件：**

- [x] 用户确认将目标与最低兼容 API 从 12 升至 20。
- [x] 缺少或格式无效的 `agentId` 时显示“能力待配置”，不生成假回复。
- [x] 用户提供的 `agentId` 已配置并通过格式校验与 ArkTS 编译。
- [x] 适配层使用 `isAgentSupport` 做可用性检查并处理隐私协议、账号、网络和内部错误。
- [x] AI 只解释方言、推荐群组和整理已校验数据；加入、退出、发布等动作回到固定 UI 确认。
- [x] 不传输精确位置、原始录音、Token 或无关用户资料。

**验证：**

- [x] API 20 下的适配层与回退状态构建通过。
- [x] 当前设备完成 `isAgentSupport` 实测并返回不支持，界面显示原因与重试且不生成假回复。
- [ ] 只有计划支持的目标设备完成账号、协议、网络和 `isAgentSupport` 实测后，才标记真实可用。

**依赖：** 用户的小艺开放平台 `agentId`；目标 API 决策；任务 14
**预计文件：** 小艺模型、适配服务、入口组件、构建配置和测试
**规模：** M

## 任务 17：真实数据导入与校验

**描述：** 先把用户提供的出版资料安全接入为来源目录；后续按带 `schemaVersion`、`sourceName` 和 `updatedAt` 的数据信封接收结构化 JSON，先只读校验，再替换相应演示数据。

**验收条件：**

- [x] 6 类资料建立题名、作者/机构、版本、用途、限制、授权和复核字段。
- [x] 原始 PDF、EPUB、地图截图和受限论文全文未打包进 HAP。
- [x] 来源目录只在内部记录“资料入口 / 授权待核验 / 未打包原文”，前台不显示资料依据入口，现有分类继续标记演示。
- [ ] 唯一 ID、必需字符串、非负成员数、长度和列表数量均有边界校验。
- [ ] 单条错误能定位到记录和字段，不把未校验内容渲染为真实数据。
- [ ] 内部导入报告记录来源、更新时间及演示/真实状态；不新增前台“资料依据”入口。
- [ ] 个人信息、精确地址或录音数据进入工程前先确认授权与脱敏。

**验证：**

- [ ] 解析成功、缺字段、重复 ID、越界数值和超量列表测试编译通过。
- [ ] 用户数据到达后生成一份只读导入报告。

**依赖：** 用户已提供研究资料目录；可替换业务模型的结构化数据仍待提供；任务 15
**预计文件：** 数据合同、适配器、校验测试、来源展示
**规模：** M

## 任务 18：鉴定页真实录音闭环

**描述：** 完善鉴定页的真实麦克风权限、采集质量、计时、实时反馈、取消、
重录、试听、中断和离页释放，同时继续明确“真实录音 + 固定模拟鉴定”的边界。

**验收条件：**

- [x] 只在首次未决定状态弹出麦克风授权；已拒绝时进入系统设置恢复，受限和无效状态给出不可用说明。
- [x] 录音中显示真实 PCM 振幅波形与声音偏小/过大文字反馈，最长 15 秒自动停止。
- [x] 少于 2 秒、静音、声音过弱和明显削波失真的录音不能进入模拟鉴定。
- [x] 停止采集后再解绑监听；试听使用当前 `writeData` 缓冲回调并在结束前排空，重录等待资源释放。
- [x] 取消、不合格、中断、离页或进入模拟鉴定时清空 PCM，不写文件、不上传。
- [x] 播放、重录、取消、自动停止和中断使用统一操作锁，避免重复释放或状态竞争。
- [x] 录音内容使用可滚动布局，波形不单独承担状态语义。

**验证：**

- [x] 录音质量纯逻辑测试已写入并编译进 `ohosTest` HAP。
- [x] 主 HAP 与 `ohosTest` HAP 通过当前 DevEco/Hvigor 编译打包。
- [ ] `ohosTest` 用例仍需在设备实际执行。
- [ ] 首次授权、拒绝与设置恢复、总开关关闭、过短、静音、正常、过强、
      15 秒自动停止、试听、重录、取消、离页和通话中断需当前构建真机复验。
- [ ] 手机竖屏、窄高窗口、Dark、大字体和读屏仍需当前构建视觉/操作检查。

**依赖：** 任务 5、任务 8
**预计文件：** 录音服务、质量判断、鉴定页、测试与交付文档
**规模：** M

## 暂缓队列：签名、真机与发布前验收

> 用户已明确这些步骤之后再做；在当前优先队列完成前不主动执行。

### 第 1 步：恢复本机可安装签名

- [x] DevEco 已为当前工程配置本机 debug 签名。
- [x] 签名证书路径和口令只用于本机调试，文档与输出不打印具体内容；`build-profile.json5` 不得提交或分享。
- [x] 已生成 signed 主 HAP 与 signed `ohosTest` HAP，并记录产物路径、大小与 SHA-256。

**完成标志：** 当前源码生成可安装的 signed HAP，敏感字段扫描仍无命中。

### 第 2 步：连接设备并实际运行自动化测试

- [x] 使用 HDC 确认设备 `2PN6R24402004396` 在线。
- [x] 安装当前 signed 主 HAP 和 `ohosTest` HAP。
- [x] 实际执行 11 个 Hypium 测试，结果为 11 通过、0 失败、0 错误。

**完成标志：** 有测试执行结果，不再只以测试 HAP 编译成功代替测试通过。

### 第 3 步：复验本轮核心交互

- [ ] 清除应用数据后首次启动，确认“我的乡音护照”处于禁用状态且没有默认四川护照。
- [ ] 分别走通“确认四川”“纠正重庆”“重新录制”三条路径。
- [ ] 验证系统返回依次关闭分享预览、纠正面板、鉴定页和护照页。
- [ ] 再次鉴定为不同地区，确认 Map Kit Marker 的图标、摘要和层级同步刷新。
- [ ] 重启应用，确认 Preferences 恢复的护照和点亮地区一致。

**完成标志：** 每条路径都有当前构建的点击记录或截图，且无领域字段混用。

### 第 4 步：补齐发布前设备验收

- [ ] 复验定位拒绝、系统设置重新授权、定位失败和本地地图兜底。
- [ ] 检查手机竖屏、横屏、600/839/840vp 边界、Dark、大字体、键盘焦点和读屏。
- [ ] 验证群聊默认“会话”、推荐/按方言发现入口、加入后发言、退出禁用、打开即已读、100 条满额和 `RESIZE_WITH_CARET` 恢复。
- [ ] 修复或正式记录 `ohosTest` 资源重复声明警告。
- [ ] 安装 `cjc/cjpm/cjfmt/cjlint` 后编译和测试 `cangjie-domain/`；在此之前继续标记为未验证。

**完成标志：** 当前构建具备安装、运行、交互和视觉证据；仓颉状态单独据实报告。
# 上架功能完善（2026-07-27）

- [x] 本地清除乡音护照和点亮记录，并显示保存、恢复、清除失败。
- [x] 增加隐私与关于页面：数据用途、权限、版本、模拟边界和清除入口。
- [x] 权限拒绝后提供系统设置恢复入口。
- [x] 地图点位驱动当前地区筛选，并提供手动地区选择。
- [x] 录音覆盖过短、占用、中断、取消、重新录制、波形、计时和播放。
- [x] 护照生成可分享图片并打开系统分享面板。
- [x] 方言分类显示来源、版本、授权和争议边界。
- [x] 首次进入提供轻量引导并可跳过。
- [x] 平板宽窗口实现地图、列表、护照主从布局。
- [ ] 主 HAP、测试、安装、运行、交互和视觉证据分层记录。

# 1.0.0 发布候选收口（2026-08-03）

- [x] `versionName` 升级为 1.0.0，release 明确 `debug=false`。
- [x] 当前工作树移除签名配置，release 启用顶层混淆、压缩和日志移除。
- [x] 修复 `AudioRenderer.write()` 与 `AlertDialog.show()` 弃用调用。
- [x] 用户可见流程移除虚构距离、成员数、关注量、假播放和假举报。
- [x] 预置人物、话题和消息统一标注为本机样例。
- [x] 生成不预裁圆角的 1024×1024 前景/背景图标资产。
- [x] 补齐隐私政策草案、上架文案、更新记录、就绪清单和回滚方案。
- [ ] 轮换/撤销历史中暴露的调试签名材料；历史重写另行授权。
- [ ] 由发布负责人补齐运营主体、联系邮箱和隐私政策公网 URL。
- [ ] 完成地点代表点第二人、纸书定位页与出版授权边界复核。
- [ ] 使用正式证书生成签名 `.app` 并上传 AppGallery Connect。
- [ ] 完成当前 1.0.0 候选的手机/平板 release gate。
