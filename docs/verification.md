# 验证记录

最近更新：2026-07-25

## 2026-07-25 当前静态与构建验证

- Light/Dark 主题的 43 个语义色 Token 集合已对齐；对比度工具覆盖 54 组
  文本、按钮、焦点、活动填充和头像组合，当前全部通过。
- 受跟踪的 `build-profile.json5` 已清除个人签名配置；工程源码与配置的
  签名路径、证书文件和口令字段扫描无命中。
- 主 HAP 完成 Hvigor 类型检查、ArkTS 编译、资源处理、打包与签名，
  `BUILD SUCCESSFUL in 25 s 316 ms`。
- `ohosTest` HAP 完成测试 ArkTS 编译和 unsigned 打包，
  `BUILD SUCCESSFUL in 17 s 848 ms`。
- 本轮未配置签名、未安装 HAP、未启动应用，也未在设备执行测试；历史 signed
  HAP、真机截图和测试结果不能替代当前源码的运行证据。
- 瞬时录音服务对监听注册、停止、释放以及麦克风权限/总开关查询异常提供显式
  清理和可恢复提示；本轮只完成编译验证，仍需真机录音复验。

## 能力边界检查

- `entry/src/main/module.json5` 只声明 Map Kit 联网所需的 `INTERNET`、`GET_NETWORK_INFO`，以及前台 `LOCATION`、`APPROXIMATELY_LOCATION`。
- 定位权限只在用户点击“我的位置”后动态申请；拒绝或异常不阻断地图，同一次运行不循环请求。
- 应用声明场景化麦克风权限；PCM 仅瞬时读取并在停止或离页后释放，不写文件、不上传。
- 历史真机调试曾由 DevEco 写入本机 debug 签名；当前受跟踪配置已清除个人签名材料。
- 当前签名对应的新应用身份访问 Map Kit 在线瓦片返回 `403 / no map permission`；错误监听和加载超时会自动切换到本地地图，本地地图的拖动、缩放和五个点位已完成真机复验。
- 录音采集为真实麦克风输入；鉴定和同乡数据仍为本地模拟。
- 群组、成员数和近期话题为本地演示数据；加入/退出只修改本次内存状态。
- 小艺代码使用官方 `@kit.AgentFrameworkKit`；用户提供的公开 `agentId` 已配置并通过格式校验，不包含 Token 或密钥。
- 6 条方言资料记录只包含内部来源元数据，全部保持“授权待核验 / 资料入口 / 未打包原文”；前台已移除资料依据入口，扫描 PDF、EPUB、地图截图和受限论文正文未进入 HAP。
- 未硬编码 Client ID、Token、API Key、证书指纹、AI API、后端地址或用户隐私数据。

## 2026-07-24 当前构建与真机状态

- 签名主 HAP：Hvigor `BUILD SUCCESSFUL in 32 s 979 ms`。
  - 路径：`entry/build/default/outputs/default/entry-default-signed.hap`
  - 大小：3,020,348 bytes
  - SHA-256：`07cc41d8618b71bd86683daa2ace94bfcf39f4a7e2e1a06d588d9d506f61234a`
  - 设备 `2PN6R24402004396` 覆盖安装成功，`EntryAbility` 启动成功。
- 签名 `ohosTest` HAP：Hvigor `BUILD SUCCESSFUL in 2 min 28 s 570 ms`。
  - 路径：`entry/build/default/outputs/ohosTest/entry-ohosTest-signed.hap`
  - 大小：3,723,051 bytes
  - SHA-256：`1179ecd3612ec091c22228a957f8b6f95ca388220d831d00b8ea22b62e3d632f`
  - 已安装到同一设备，并通过 `OpenHarmonyTestRunner` 实际执行。
  - 结果：`Tests run: 11, Failure: 0, Error: 0, Pass: 11, Ignore: 0`。
- 以下签名与真机结果均为 2026-07-24 历史证据；当前受跟踪配置不包含个人签名材料。
- 真机截图已确认：
  - 当前 Map Kit 权限失败后自动显示本地地图，横竖屏均有五个乡音点位、拖动/缩放提示和重试入口。
  - 首页只保留“同乡 / 乡音群组”，不再显示资料依据页签。
  - 群组横屏列表显示推荐重点卡、紧凑列表行、已加入数量和本机演示边界。
  - 群组详情展示身份摘要、成员/话题概览、默认近期话题和可切换群组约定。
  - 退出群组弹层会说明只移除本机演示状态；确认后按钮变为“加入群组”，返回列表的已加入数量从 1 更新为 0。
  - 系统返回从群组详情回到地图与群组列表。
  - 小艺官方能力检查返回“当前设备不可用”，界面显示原因和重试，没有生成本地假回复。

## 2026-07-24 质感、群组、小艺与内部资料目录增量

- `build-profile.json5` 的目标与最低兼容版本均升级为 HarmonyOS 6.0.0 / API 20。
- 地图底部面板只保留固定的“同乡 / 乡音群组”一级切换；资料目录退回内部数据参考。手机继续使用底部面板，宽窗口继续使用地图 + 侧栏。
- Map Kit 初始化已设置 `sphereEnabled: true`，并将初始缩放设为 2.6，以满足官方指南中“API 15 起支持、缩放级别小于 4 时可明显看到球面”的 3D 地球展示条件。该项目前仅有源码与构建证据；在线球面底图仍需在具备 Map Kit 权限的真机环境验证。
- 首页半模态在透明模糊底层之上增加低透明冷暖环境色渐变、高光边缘和轻阴影；拖拽过程保持逐帧跟手，松手吸附使用当前 SDK 的 `responsiveSpringMotion`，一级与二级栏目切换使用 150ms 小范围状态过渡。
- 群组提供推荐、已加入和按方言浏览；推荐首项与普通行形成主次，详情默认突出近期话题并可切换群组约定。
- 加入/退出必须经过确认弹层，只更新本次本机状态，并持续提示尚未同步服务器。
- 小艺适配层直接引用官方 `@kit.AgentFrameworkKit`，实现未配置、检查中、不支持、可用和错误状态。
- 只有 `isAgentSupport` 返回 `true` 时才渲染官方 `FunctionComponent`；参数、隐私协议、华为账号和网络错误都有明确回退。
- 当前 `XIAOYI_AGENT_ID` 已配置为用户提供的 `agent67ed6c01203b437db99567238ebdc976`；只有官方 `isAgentSupport` 返回 `true` 才显示组件，尚无当前源码的真机可用性证据。
- 新增群组筛选、成员状态切换、小艺配置和来源准入判断测试；11 个测试已在设备执行并全部通过。
- 用户提供的方言资料已接入 6 条只读目录记录；因缺少页码、版本年份、授权和人工复核，没有替换任何演示分类、模拟鉴定或群组数据。

## 2026-07-23 历史构建状态

- 主 HAP：Hvigor `BUILD SUCCESSFUL`。
  - 路径：`entry/build/default/outputs/default/entry-default-signed.hap`
  - 大小：2,761,770 bytes
  - SHA-256：`f7c6b193b19c7b2a311966121600198fbc308090a1cc99c4b5e676fa440fdef0`
- `ohosTest` HAP：Hvigor `BUILD SUCCESSFUL`。
  - 路径：`entry/build/default/outputs/ohosTest/entry-ohosTest-signed.hap`
  - 大小：3,662,754 bytes
  - SHA-256：`0fae20b77d144dd61382dc95e1810a58ae04894afaa0d315974ec105f7fc778d`
- 主 HAP 与 `ohosTest` HAP 当时使用用户本机 debug provision 完成签名；该签名配置现已从受跟踪文件清除。
- `ohosTest` 仅完成编译打包，尚未在设备执行。

## 运行状态

设备 `2PN6R24402004396` 已完成安装、启动和核心流程复验。当前签名身份的
Map Kit 在线地图与 Marker 已恢复。真实麦克风允许路径完成 4 秒有效 PCM
采集，停止后释放且未写文件；AI 鉴定仍是明确标注的固定模拟数据，不属于
真实识别能力证明。

## 可信鉴定与持久化增量

- 模拟鉴定结果现包含方言大区、分支、候选地域、识别依据和相似乡音。
- 用户必须确认结果、选择纠正结果或重新录制，未经确认不能点亮地区。
- Preferences 只保存地区、方言和分支，不保存原始录音或精确位置。
- 读取或写入异常会降级为当前会话状态，不阻断鉴定和护照流程。
- 地图点位增加独立“已点亮”图标；本地兜底地图同时显示文字状态。
- 用户确认、纠正为“重庆话 · 成渝片”、生成个人护照、地图点亮图标和覆盖安装后恢复均已通过真机验证。

## 2026-07-23 真机面板复验

- 设备 `2PN6R24402004396` 成功安装并启动 `com.dialectmap.app` 0.1.0。
- 包管理信息确认应用为 HarmonyOS Stage 模型、debug provision，目标与兼容版本为 5.0.0(12)。
- Map Kit 3D 地球、五个乡音点位和“我的位置”入口已在真机显示。
- 同乡面板紧凑态约 220vp，真机截图未再出现原42%收起态的大面积空白。
- 通过设备输入完成紧凑态向上拖动、充分展开向下拖动，两个方向均成功吸附。
- 从同乡入口打开乡音护照成功，系统返回可回到地图。
- 首次位置权限弹窗真实显示用途说明及允许/不允许选项；选择“仅使用期间允许”后，蓝色当前位置标记、镜头居中和“已定位到你当前所在的位置”均已复验。

## 2026-07-23 鉴定主链与恢复复验

- 固定模拟结果正确展示方言分支、置信度、保留度、候选地域、识别依据和模拟能力声明。
- 点击“不太准确”后可选择“重庆话 · 成渝片”，页面显示“已由你确认”，未经确认不能提前点亮。
- 点击“点亮重庆并生成乡音护照”后进入当前用户护照，包含头像/昵称、所在地、方言归属、87% 置信度、76% 保留度、代表语音、已点亮地区、同乡好友、关注、被关注和分享入口。
- 系统返回回到地图；新增重庆点位并以绿色勾选图标表达“已点亮”，不只依赖颜色。
- 使用 `hdc install -r` 覆盖安装并重新启动应用后，重庆点亮图标仍显示；打开“我的乡音护照”仍恢复重庆话、重庆地区、87% 和 76%，证明 Preferences 持久化生效。
- 真机修复后重新执行 Hvigor，`BUILD SUCCESSFUL in 27 s 469 ms`。

## 仓颉状态

本机未检测到 `cjc` 或 `cjpm`。`cangjie-domain/` 已完成结构与静态检查，但尚未由仓颉编译器编译，也未运行仓颉单元测试。

## 2026-07-24 方言分类模型增量

- 新增统一分类契约：方言大类、方言片区、地方口音和代表地区。
- 四川话与重庆话归入“官话 → 西南官话 → 成渝片”；上海话归入“吴语 → 太湖片 → 苏沪嘉小片”；北京话归入“官话 → 北京官话 → 京师片”。
- 粤语演示节点改用广州作为代表地区，不再把广东整体等同于单一粤语区。
- 地图 Marker、鉴定结果、确认按钮、底部点亮进度和乡音护照已接入同一分类字段。
- 所有节点持续标记为产品演示分类；正式方言区边界、资料来源、版本与授权仍未接入。
- 主 HAP 与 `ohosTest` HAP 均通过 Hvigor 编译；signed 主 HAP 已覆盖安装到设备 `2PN6R24402004396`。
- 横屏真机已确认首页显示“1 / 4 个演示片区已点亮”，护照显示“官话 · 西南官话 · 成渝片”，模拟鉴定结果显示相同三级归属且无截断。

## 2026-07-24 逻辑一致性与安全修复

- 当前用户初始状态改为空；只有 Preferences 恢复成功或鉴定确认完成后才生成“我的乡音护照”。
- 普通确认会把领域对象的 `needsConfirmation` 改为 `false`。
- 纠正结果从目标分类重新生成候选地域、识别依据和相似乡音；四川与重庆详情不再混用。
- 分享预览和鉴定纠正面板状态提升到首页；系统返回按临时顶层状态、次级页面的顺序关闭。
- Map Kit Marker 保存实例，并使用当前 SDK 已声明的 `setPosition`、`setIcon`、`setTitle`、`setSnippet` 和 `setZIndex` 刷新点亮状态。
- 当前主 HAP 已通过 Hvigor 类型检查、签名打包和设备覆盖安装。
- 当前 `ohosTest` HAP 已通过构建、签名安装和设备执行；11 个测试全部通过。
- 群组相关返回和成员状态已有当前构建设备证据；鉴定纠正详情和 Marker 再次刷新仍需按原核心交互队列复验。
